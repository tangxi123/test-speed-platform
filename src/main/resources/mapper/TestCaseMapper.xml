<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.tangxi.testplatform.mapper.TestCaseMapper">
    <insert id="createTestCase" parameterType="org.tangxi.testplatform.model.TestCase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO zsi_test_case(test_name, method, url, headers, parameters, created_at, updated_at, suite,
        test_module, descs)
        VALUES (#{testName},
        #{method},
        #{url},
        #{headers},
        #{parameters},
        #{createdAt},
        #{updatedAt},
        #{suite},
        #{testModule },
        #{descs})

    </insert>

</mapper>